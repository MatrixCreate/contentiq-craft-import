{% extends '_layouts/cp' %}

{% set title = 'Import History' %}

{% set crumbs = [
    { label: 'ContentIQ', url: url('contentiq-importer') },
] %}

{% block actionButton %}
    <a class="btn submit add icon" href="{{ url('contentiq-importer/upload') }}">
        {{ 'New Import'|t('contentiq-importer') }}
    </a>
{% endblock %}

{% block content %}

    {% if runs|length %}
        <table class="data fullwidth">
            <thead>
                <tr>
                    <th>{{ 'Date'|t('contentiq-importer') }}</th>
                    <th>{{ 'File'|t('contentiq-importer') }}</th>
                    <th>{{ 'Type'|t('contentiq-importer') }}</th>
                    <th>{{ 'Pages'|t('contentiq-importer') }}</th>
                    <th>{{ 'Images'|t('contentiq-importer') }}</th>
                    <th>{{ 'Status'|t('contentiq-importer') }}</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for run in runs %}
                    <tr>
                        <td>{{ run.dateCreated|datetime('short') }}</td>
                        <td><code>{{ run.filename }}</code></td>
                        <td>
                            {% if run.type == 'batch' %}
                                <span class="status" style="color:var(--ui-control-color)"></span> Batch
                            {% elseif run.type == 'sync' %}
                                <span class="status blue"></span> Sync
                            {% else %}
                                Single
                            {% endif %}
                        </td>
                        <td>{{ run.pageCount }}</td>
                        <td>{{ run.imageCount }}</td>
                        <td>
                            {% if run.status == 'success' %}
                                <span class="status green"></span> {{ 'Success'|t('contentiq-importer') }}
                            {% elseif run.status == 'warnings' %}
                                <span class="status orange"></span> {{ 'Warnings'|t('contentiq-importer') }}
                            {% elseif run.status == 'pending' %}
                                <span class="status"></span> {{ 'In Progress'|t('contentiq-importer') }}
                            {% else %}
                                <span class="status red"></span> {{ 'Errors'|t('contentiq-importer') }}
                            {% endif %}
                        </td>
                        <td class="thin">
                            <a href="{{ url('contentiq-importer/result/' ~ run.id) }}" class="btn small">
                                {{ 'View'|t('contentiq-importer') }}
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <div class="zilch">
            <p>{{ 'No imports yet.'|t('contentiq-importer') }}</p>
        </div>
    {% endif %}

{% endblock %}
